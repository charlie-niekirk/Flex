<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:foreground="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true">

    <ImageView
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:id="@+id/settings_section_icon"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginStart="@dimen/spacing_l"
        android:layout_marginVertical="@dimen/spacing_l"
        android:contentDescription="@string/settings_section_icon_cd"
        tools:src="@drawable/view_list" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/settings_section_title"
        app:layout_constraintStart_toEndOf="@id/settings_section_icon"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/settings_section_subtitle"
        android:layout_marginStart="@dimen/spacing_l"
        app:layout_constraintVertical_chainStyle="packed"
        android:textStyle="bold"
        tools:text="Post view" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/settings_section_subtitle"
        app:layout_constraintStart_toStartOf="@id/settings_section_title"
        app:layout_constraintTop_toBottomOf="@id/settings_section_title"
        app:layout_constraintBottom_toBottomOf="parent"
        tools:text="Font size, preview style and more" />

</androidx.constraintlayout.widget.ConstraintLayout>